# NewChainSafe — .env de exemplo para PoC local
# Copiar para .env e ajustar. NUNCA commitar `.env` real (ver .gitignore).
# Escopo minimo para INF-02 subir a stack.
# INF-03 expande com SSO Azure AD, admin token, SMTP producao etc.

# --- PostgreSQL --------------------------------------------------------------
POSTGRES_DB=vaultwarden
POSTGRES_USER=vaultwarden
POSTGRES_PASSWORD=troque-este-valor-por-um-forte
POSTGRES_PORT=5432

# --- Vaultwarden -------------------------------------------------------------
# URL publica que os clientes usam. Em PoC local, http://localhost:<port>.
DOMAIN=http://localhost:8080
VAULTWARDEN_PORT=8080

# Cadastro local desabilitado — apenas SSO Azure AD (INF-05).
SIGNUPS_ALLOWED=false

# --- MailDev (SMTP local de teste) -------------------------------------------
MAILDEV_UI_PORT=1080
MAILDEV_SMTP_PORT=1025
SMTP_FROM=vaultwarden@novacorrente.local
SMTP_FROM_NAME=NewChainSafe

# --- ADMIN_TOKEN -------------------------------------------------------------
# Deixe vazio nesta etapa (admin backend desabilitado). INF-03 documenta como
# gerar um token seguro (argon2/bcrypt) quando for necessario habilitar.
# ADMIN_TOKEN=

# --- SSO Azure AD ------------------------------------------------------------
# Preenchido em INF-04 (App Registration) + INF-05 (config).
# SSO_ENABLED=true
# SSO_ONLY=false        # NAO ativar — BUG-01 (redirect loop).
# SSO_CLIENT_ID=
# SSO_CLIENT_SECRET=
# SSO_AUTHORITY=https://login.microsoftonline.com/<tenant-id>/v2.0
# SSO_SCOPES=email profile
# SIGNUPS_DOMAINS_WHITELIST=novacorrente.ind.br
